version: "3.8"
services:
  app:
    build: .
    image: payflow-api:latest
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: local
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
